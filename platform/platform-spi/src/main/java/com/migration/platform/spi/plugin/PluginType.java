package com.migration.platform.spi.plugin;

/**
 * Plugin type registry keys used in plugin.json.
 */
public enum PluginType {
    READER,
    WRITER,
    LOG_ADAPTER,
    TYPE_MAPPER,
    TRANSFORMER
}
